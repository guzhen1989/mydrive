# 快速开始指南

## 前置要求

### 必需软件
1. **Node.js** (v16 或更高)
   ```bash
   node --version  # 检查版本
   ```

2. **Rust** (1.70 或更高)
   ```bash
   rustc --version  # 检查版本
   ```
   
   如果未安装 Rust:
   ```bash
   curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
   ```

3. **MinIO 服务器** (用于测试)
   
   使用 Docker 快速启动:
   ```bash
   docker run -p 9000:9000 -p 9001:9001 \
     -e "MINIO_ROOT_USER=minioadmin" \
     -e "MINIO_ROOT_PASSWORD=minioadmin" \
     quay.io/minio/minio server /data --console-address ":9001"
   ```

## 安装步骤

### 1. 克隆或下载项目

```bash
cd minio-desktop
```

### 2. 安装依赖

```bash
# 安装前端 npm 依赖
npm install
```

Rust 依赖会在首次运行时自动下载。

### 3. 启动开发服务器

```bash
npm run tauri dev
```

首次运行可能需要较长时间(5-10分钟),因为需要:
- 下载 Rust 依赖包
- 编译 Rust 代码
- 编译 Tauri 运行时

### 4. 连接到 MinIO

应用启动后,你会看到连接配置界面:

1. **服务器地址**: `localhost`
2. **端口**: `9000`
3. **Access Key**: `minioadmin`
4. **Secret Key**: `minioadmin`
5. **使用 SSL**: 取消勾选(本地测试不需要)

点击"测试连接"验证配置,然后点击"连接"。

## 功能测试

### 创建存储桶
1. 点击左侧边栏顶部的 "+" 按钮
2. 输入存储桶名称(如: `test-bucket`)
3. 点击"创建"

### 浏览存储桶
1. 点击存储桶名称
2. 查看存储桶内的对象列表
3. 双击文件夹进入子目录

### 删除对象
1. 在对象列表中找到要删除的对象
2. 点击右侧的"删除"按钮
3. 确认删除

## 构建生产版本

```bash
npm run tauri build
```

构建产物位置:
- **macOS**: `src-tauri/target/release/bundle/dmg/`
- **Windows**: `src-tauri/target/release/bundle/msi/`
- **Linux**: `src-tauri/target/release/bundle/deb/` 或 `appimage/`

## 常见问题

### Q: 编译失败,提示 Rust 工具链问题
A: 确保安装了最新的 Rust 工具链:
```bash
rustup update
```

### Q: npm install 失败
A: 尝试清除缓存:
```bash
rm -rf node_modules package-lock.json
npm install
```

### Q: 无法连接到 MinIO
A: 检查:
1. MinIO 服务器是否正在运行
2. 端口是否正确
3. 访问密钥是否正确
4. 防火墙设置

### Q: 应用窗口显示空白
A: 检查浏览器控制台(开发模式下按 F12),查看是否有 JavaScript 错误。

### Q: Rust 编译太慢
A: 首次编译会下载和编译大量依赖,这是正常的。后续的增量编译会快很多。可以考虑:
```bash
# 使用更快的链接器 (macOS)
brew install llvm
```

## 开发提示

### 热重载
- **前端代码修改**: 自动热重载
- **Rust 代码修改**: 需要重新编译(会自动触发)

### 调试
- **前端调试**: 在开发模式下按 F12 打开开发者工具
- **后端调试**: 查看终端输出的 Rust 日志

### 数据库位置
- **macOS**: `~/Library/Application Support/minio-desktop/data.db`
- **Windows**: `%APPDATA%\minio-desktop\data.db`
- **Linux**: `~/.local/share/minio-desktop/data.db`

如需重置数据库,删除此文件即可。

## 下一步

- 阅读 [README.md](README.md) 了解完整功能
- 查看 [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md) 了解项目架构
- 参考设计文档了解实现细节

## 获取帮助

如遇到问题:
1. 检查终端输出的错误信息
2. 查看浏览器控制台
3. 查阅 Tauri 官方文档: https://tauri.app/
4. 提交 Issue

祝使用愉快! 🚀
